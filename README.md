```
$ node index.js exec --help
Usage: ovmwd exec [options]

execute OVM1 withdrawals

Options:
  --l1-token <token>  l1 token address or "eth"
  --l2-token <token>  l2 token address or "eth"
  --amount <amount>   amount to withdraw
  --from <from>       from address
  --to <to>           to address
  --nonce <nonce>     message nonce
  --pk <key>          private key to prove or relay with
  -h, --help          display help for command
```
